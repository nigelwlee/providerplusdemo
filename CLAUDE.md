@AGENTS.md

# Provider+ Demo

Before any UI/design work, read `DESIGN_GUIDELINES.md` — it has the real
Provider+ brand tokens (colors, type, spacing, components) extracted from
providerplus.com.au. All visual decisions should resolve to that palette.
